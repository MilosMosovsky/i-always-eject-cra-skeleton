import * as React from "react";

import Counter from "../../domains/common/components/counter";
import Template from "../../templates/default";

export default () => (
  <Template>
    <h1>Main Page</h1>

    <Counter />
  </Template>
);
